---
name: mathmodel-data-tools
description: 数学建模国赛数据处理工具链。提供三件套：数据画像生成器（让 AI 不读全量数据也能看懂数据结构）、按需查询助手（精确取数，不靠记忆和摘要）、L1 中间层构建器（把任意 xlsx/csv 预聚合成缓存表，建模脚本秒级读取）。当赛题附件数据量大（万行以上）、需要理解数据结构、需要精确统计量、或建模脚本反复读取原始大文件很慢时使用。适用于任何年份、任何赛题的数据，不绑定特定题目。
---

# 数学建模国赛数据处理工具链

解决国赛数据类赛题（尤其 C 题）的三个核心痛点：

| 痛点 | 后果 | 本工具链的对策 |
| --- | --- | --- |
| 数据几十万行，AI 无法全读 | 只能看片段，产生幻觉 | **画像生成器**：一次生成结构化 `.md` 画像 |
| 靠摘要里的数字写论文 | 数值前后矛盾，硬伤 | **查询助手**：每个数字现场从原始数据算 |
| 建模脚本反复读 xlsx | 每次几十秒到几分钟 | **L1 中间层**：预聚合缓存，毫秒级读取 |

## 四层数据架构

```
L0  数据字典（.md）      画像生成器产出，AI 读它了解数据结构
L1  中间层聚合表（.csv） l1_build 产出，建模脚本的数据源
L2  原始明细（.xlsx）    只在需要精确取数时按需查询
L3  结果表（.json/.csv） 建模产出，论文引用的数值来源
```

**核心原则**：摘要用于**理解**，绝不用于**计算**。任何写进论文的数字，必须来自 L1/L2 的现场计算或 L3 的结果表。

## 工具一：数据画像生成器

一条命令把任意数据文件变成 AI 可读的结构化画像。

```bash
python tools/gen_data_profile.py <数据文件> --label "语义标签" --group "分组列" --out 画像.md
```

产出包含：行列规模、每列 dtype/缺失率/基数、数值列分位数、分类列取值分布、分组聚合概览、样本行。

## 工具二：按需查询助手

需要精确数字时现场算，不依赖任何摘要。

```bash
python tools/query_data.py describe <文件>                          # 整体描述
python tools/query_data.py group   <文件> --by 列 --agg "列:sum,mean"  # 分组聚合
python tools/query_data.py filter  <文件> --where "年份 == '第5年'"     # 条件筛选
python tools/query_data.py expr    <文件> "df[df['量'] > df['量'].mean()].shape[0]"  # 任意表达式
```

> **中文列名注意**：含空格、括号或特殊字符的列名（如 `批发价格(元/千克)`）在 `--where` 中**必须用反引号包裹**，否则 pandas 会当函数调用解析：
> ```
> --where '`批发价格(元/千克)`>=0.5'
> ```

## 工具三：L1 中间层构建器

把原始数据预聚合成缓存表，建模脚本从此秒级读取。**不假设任何字段名**，靠自动检测 + 可覆盖。

```bash
# 最简：全自动检测
python tools/l1_build.py 附件1.xlsx 附件2.xlsx --out l1

# 显式指定角色 + 过滤
python tools/l1_build.py 附件2.xlsx --out l1 --date 销售日期 \
    --group 单品编码 --measures "销量(千克),销售单价(元/千克)" \
    --where '`销售类型`=="销售"'
```

### 自动检测规则

| 角色 | 判定规则 |
| --- | --- |
| 日期列 | datetime 类型，或字符串解析成功率 ≥70% **且年份落在 1900–2100**（防止长数字编码被误判） |
| 度量列 | 数值列，排除 id 列与标记列 |
| id 列 | 列名含 `编码/编号/id/code`，或高基数整型/量级 ≥1e9（连续浮点价格不会误伤） |
| 标记列 | 取值为 0/1 二值，或低基数且列名像 `是否/标志/flag/折扣` |
| 分组列 | 低基数（<200）文本列；`--group` 可显式覆盖 |

关键角色均可用 `--date / --group / --measures / --where` 覆盖自动检测结果。

### 产出（`l1/<文件主名>/`）

| 文件 | 内容 |
| --- | --- |
| `_dictionary.md` | 该文件的数据字典 |
| `daily.csv` / `weekly.csv` / `monthly.csv` | 按日/周/月聚合（sum/mean/count） |
| `daily_by_group.csv` | 日期 × 分组的长表 |
| `_map.csv` | 维度表映射（无日期无度量的文件自动产出） |
| `_vc_<列>.csv` | 该列取值计数 |
| `_meta.json` | 角色检测结果与构建参数 |

### 建模脚本读取方式

```python
from tools.l1_loader import load, meta, available, dictionary

df   = load("l1", "附件2", table="daily_by_group")   # 秒级读取
info = meta("l1", "附件2")                           # 看角色检测结果
tabs = available("l1", "附件2")                      # 有哪些表可用
```

## 使用时机

在国赛工作流中的位置：**阶段 1（读题）与阶段 2（`/2analysis-modeling` 数据理解）之间，以及整个建模过程中**。

1. 拿到附件 → 跑画像生成器，读画像看懂数据结构
2. 建模前 → 跑 L1 构建器，为建模脚本准备快速数据源
3. 建模中 → 脚本用 `l1_loader` 读 L1 表
4. 写论文时 → 任何数字用查询助手现场核验，或引用 L3 结果表

## 自检

```bash
python tools/test_tools.py
```

7 项冒烟测试覆盖编码识别、画像生成、四种查询模式、L1 事实表与维度表构建。

## 配套文档

- `tools/README.md` — 三个工具的完整命令参考
- `数据应对手册.md` — 数据处理的完整战术流程（任何赛题通用）
- `math_modeling_norms.md` — 防幻觉、数值一致性等规范细则
