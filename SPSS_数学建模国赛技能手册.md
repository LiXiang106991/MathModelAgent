# SPSS 数学建模国赛技能手册
## 技能适用场景
当遇到以下情况时调用本技能对应模块：
- 需要在 SPSS 中完成某项统计分析操作 → 【模块一】
- 拿到 SPSS 输出表格看不懂 → 【模块二】
- 需要把分析结果写进论文 → 【模块三】
- 不确定赛题该用哪种统计方法 → 【模块四】
- 需要批量/复现分析流程 → 【模块五】
- 犹豫用 SPSS 还是 Python/MATLAB → 【模块六】
---
## 模块一：具体操作教学
## 1.1 数据导入与预处理
### 导入 Excel 数据
. **文件 → 打开 → 数据**，文件类型选 `Excel(*.xlsx, *.xls)`
. 勾选 ✅"从第一行数据读取变量名"
. 确定后检查"数据视图"与"变量视图"
### 缺失值处理
- **路径**：转换 → 替换缺失值
- 方法选择：
- 序列均值（数据平稳时）
- 临近点均值/中位数（有局部趋势时）
- 线性插值（时间序列数据，**国赛推荐**）
### 异常值检测
- **路径**：分析 → 描述统计 → 探索
- 因变量列表放入变量 → 统计 → 勾选"离群值"
- 看箱线图：超出箱体 1.5 倍四分位距的点为离群值，超出 3 倍为极端值
### 标准化（聚类/回归前必做）
- **路径**：分析 → 描述统计 → 描述
- 勾选 ✅"将标准化值另存为变量" → 生成 `Zxxx` 新变量
## 1.2 多元线性回归 + VIF 诊断（⭐核心操作）
**完整步骤：**
. **分析 → 回归 → 线性**
. 因变量选入 `Y`，自变量选入 `X1~Xn`
. **方法**：
- "输入"（Enter）：全部强制进入，国赛默认用这个
- "步进"（Stepwise）：自动筛选变量，变量多时用
. 点击**统计**按钮，勾选：
- ✅ 估算值（回归系数及检验）
- ✅ 模型拟合（R²）
- ✅ 共线性诊断（VIF，**必勾**）
- ✅ 德宾-沃森（Durbin-Watson，检验自相关）
. 点击**图**按钮：
- ✅ 直方图（残差正态性）
- ✅ 正态概率图（P-P图）
- Y轴选 `*ZRESID`（标准化残差），X轴选 `*ZPRED`（标准化预测值）→ 残差图
. 点击**确定**，输出结果（解读见模块二）
## 1.3 假设检验类操作
### 独立样本 t 检验（两组比较）
**分析 → 比较平均值 → 独立样本t检验**
- 检验变量：连续变量
- 分组变量：定义组（如组1=1，组2=2）
### 单因素方差分析（三组及以上）
**分析 → 比较平均值 → 单因素ANOVA**
- 因变量：连续变量；因子：分组变量
- **事后比较**：勾选 Tukey（各组样本量相近）或 Scheffe / LSD
- **选项**：勾选"描述统计"和"方差齐性检验"
### 配对样本 t 检验（前后对比）
**分析 → 比较平均值 → 配对样本t检验**
### 卡方检验（分类变量间关系）

**分析 → 描述统计 → 交叉表**
- 行、列分别放两个分类变量
- 统计 → 勾选"卡方"
- 单元格 → 勾选"期望"和"观察值"
## 1.4 聚类分析
### K-Means 聚类（大样本，先确定K）
. **先标准化数据**（见1.1）
. **分析 → 分类 → K均值聚类**
. 变量选入标准化后的 `Z` 变量
. 聚类数：填 K 值
. 保存 → 勾选 ✅"聚类成员"（生成每条记录的类别标签）
. 迭代：最大迭代次数默认 10，可改 50
### 系统聚类（小样本，看树状图定K）
. **分析 → 分类 → 系统聚类**
. 聚类方法：组间联接 / Ward 法（**国赛推荐 Ward**）
. 绘制 → 勾选 ✅"谱系图"（树状图）
. 保存 → 单一解（填K）或某个解范围
## 1.5 因子分析 / 主成分分析
. **分析 → 降维 → 因子**
. 变量选入所有指标
. **描述** → 勾选 ✅"KMO和巴特利特球形度检验"、✅"初始解"
. **提取** → 方法选"主成分"，勾选"碎石图"，提取标准：特征值>1 或手动指定因子数
. **旋转** → 选"最大方差法"（Varimax）
. **得分** → 勾选 ✅"保存为变量"（方法：回归），生成因子得分用于后续建模
. **选项** → 勾选"按大小排序"（载荷表更易读）
## 1.6 问卷信度分析
**分析 → 标度 → 可靠性分析**
- 模型：α 系数（Cronbach's Alpha）
- 统计 → 勾选"删除项后的标度"（看删除某题后α是否提升）
---
## 模块二：结果解读
## 2.1 核心指标速查表
| 指标 | 位置 | 判断标准 | 含义 |
|------|------|----------|------|
| Sig.(p值) | 所有检验表 | <0.05 显著，<0.01 极显著 | 结果由偶然引起的概率 |
| R² | 模型汇总 | >0.7 好；社科类 >0.3 可接受 | 模型解释力 |
| 调整R² | 模型汇总 | 与R²差距小说明变量不冗余 | 校正后的解释力 |
| F统计量 | ANOVA表 | Sig.<0.05 | 模型整体是否显著 |
| t统计量 | 系数表 | Sig.<0.05 | 单个变量是否显著 |
| VIF | 系数表 | <10 宽松；<5 严格 | 共线性（越小越好） |
| 容忍度 | 系数表 | >0.1 | VIF的倒数 |
| Durbin-Watson | 模型汇总 | 1.5~2.5 可接受，≈2 最佳 | 残差自相关 |
| KMO | 因子分析 | >0.6 可做；>0.8 好 | 数据是否适合因子分析 |
| Bartlett Sig. | 因子分析 | <0.05 适合 | 变量间是否有相关性 |
| Cronbach's α | 信度分析 | >0.7 可接受；>0.8 好 | 问卷内部一致性 |
## 2.2 回归输出四表精读（⭐最重要的解读能力）
### 表①：输入/除去的变量
确认自变量是否全部进入模型（步进法时看筛选掉了谁）。
### 表②：模型汇总
| 关注项 | 解读 |
|--------|------|
| R | 复相关系数，越大越好 |
| R² | 例：0.782 = 模型解释了因变量78.2%的变异 |
| 标准估算误差 | 越小拟合越好 |
| Durbin-Watson | ≈2 无自相关；<1 或 >3 有问题 |
### 表③：ANOVA（F检验）
- **看什么**：回归行的 Sig. 值
- **判断**：Sig. < 0.05 → 模型整体显著，回归方程成立
- **Sig. ≥ 0.05** → 模型无意义，需换变量或换方法
### 表④：系数表（最关键）
每列含义：
| 列 | 含义 | 用途 |
|----|------|------|
| B（非标准化系数） | 原始方程系数 | 写回归方程：Y = B₀ + B₁X₁ + ... |
| 标准误差 | 系数的波动 | 报告用 |
| Beta（标准化系数） | 去量纲后的系数 | **比较哪个变量影响更大**（绝对值） |
| t / Sig. | 系数显著性 | Sig.>0.05 的变量考虑剔除 |
| 容忍度/VIF | 共线性 | **诊断决策：**

| 诊断结果 | 处理方案 |
|----------|----------|
| VIF > 10 → 存在严重共线性 | 剔除变量 / 逐步回归 / 岭回归 |
| 残差直方图偏态 → 正态性不满足 | 变量取对数 / Box-Cox变换 |
| 残差图呈喇叭形 → 异方差 | 加权最小二乘 / 变量变换 |
| DW < 1.5 → 正自相关（时间序列） | 加滞后项 |

## 2.3 其他常见输出解读
### 独立样本t检验（两行怎么看）
- **第一行"假定等方差"**：先看上方"方差齐性检验(Levene)"的 Sig.
- Levene Sig. > 0.05 → 方差齐 → 读**第一行**的 t 和 Sig.
- Levene Sig. ≤ 0.05 → 方差不齐 → 读**第二行**的 t 和 Sig.
- 最终 Sig. < 0.05 → 两组均值有显著差异
### K-Means 聚类输出
- **最终聚类中心**：每类在各变量上的均值 → 给每类"画像"命名（如"高消费高活跃型"）
- **ANOVA表**：聚类后各变量的组间差异是否显著（Sig.<0.05 说明该变量有效区分了类）
- **每个聚类中的个案数**：某类样本过少（<5%）考虑减少K值
### 因子分析输出
- **总方差解释**：看"旋转载荷平方和之和"列的累积% → >60% 可接受
- **旋转后的成分矩阵**：载荷绝对值 >0.5 归属该因子 → 给因子命名（如"经济因子""环境因子"）
- **碎石图**：拐点位置提示合理因子数
---
## 模块三：论文呈现
## 3.1 三线表规范（国赛论文硬性审美要求）
SPSS 表格复制进 Word 后必须改造：
. 删除所有竖线
. 只保留三条横线：**顶线（粗）、栏目线（细）、底线（粗）**
. Word操作：表格 → 边框 → 无框线 → 手动加三条横线
. 表标题在表格**上方**：如"表3-1 多元回归分析结果"
. 图标题在图片**下方**：如"图3-1 残差正态P-P图"
## 3.2 结果描述模板句式库（直接套用）
### 回归结果模板
> 以 Y 为因变量，X₁~X₅ 为自变量，建立多元线性回归模型。模型整体显著（F = 12.34, p < 0.001），调整R² = 0.782，表明模型可解释 Y 变异的
> Y = 2.31 + 0.45X₁ - 0.23X₃
### t检验/ANOVA模板
> 采用独立样本 t 检验分析两组差异。Levene 检验表明方差齐性（p = 0.432），t 检验结果显示两组 Y 值差异显著（t = 3.21, df = 98, p = 0
### 聚类结果模板
> 采用 K-Means 聚类将样本分为 3 类。方差分析显示各变量在类间差异均显著（p < 0.05），聚类有效。根据最终聚类中心，将三类命名为：①高特征型
### 因子分析模板
> KMO 值为 0.83（>0.8），Bartlett 球形检验显著（χ² = 253.4, p < 0.001），适合进行因子分析。采用主成分法提取因子并经最大方差旋转，提取
## 3.3 常见错误清单（论文扣分点）
| ❌ 错误 | ✅ 正确做法 |
|--------|-----------|
| 直接截图粘贴SPSS输出 | 改为三线表，规范排版 |
| 写 p = 0.000 | SPSS显示0.000实际是 p < 0.001 |
| 只报R²不报F检验 | 必须先报F检验再报R² |
| 堆方法不解释 | 每个统计量都要有一句文字解释 |
| 变量无单位、无定义 | 表格中注明变量含义和单位 |
| 不注明软件 | 加一句"数据分析采用SPSS 26.0完成" |
| 表图无编号、正文不引用 | 每张图表必须编号且正文引用（如"如表3-1所示"） |
---
## 模块四：方法选择
##### 4.1 决策流程（文字版决策树）
##### 4.2 国赛题型 → 方法映射表

. 每条命令以英文句号 . 结束
. 子命令以 / 开头
. 注释以 * 开头，以 . 结尾
. 变量名不区分大小写，不能含空格
. 打开方式：文件 → 新建 → 语法；运行：Ctrl+A 全选后 Ctrl+R
描述统计与标准化
多元回归 + 完整诊断（⭐核心模板）
逐步回归（自动筛选变量）
Logistic回归
假设检验三件套
| 国赛题型 | SPSS承担的工作 |
|----------|----------------|
| **评价类**（综合评价某地区/方案） | 因子分析定权重、聚类给对象分级 |
| **预测类**（预测未来销量/价格） | 曲线估计初探、回归基线模型（主力用Python/MATLAB） |
| **影响因素类**（什么因素影响结果） | 相关分析初筛 + 多元回归 + Logistic回归 |
| **分类分群类**（客户分类、区域划分） | K-Means / 系统聚类 / 判别分析 |
| **问卷调查类**（附件是问卷数据） | 信度 → 效度（因子）→ 描述统计 → 回归/差异检验，**全套SPSS** |
| **政策效果类**（前后对比） | 配对t检验 / 交叉表卡方 |
## 4.3 数据前提检查（做之前必查）
| 方法 | 前提条件 | 不满足时 |
|------|----------|----------|
| 线性回归 | 残差正态、方差齐、无共线性 | 取对数 / 逐步回归 / 换方法 |
| t检验/ANOVA | 正态性、方差齐 | Mann-Whitney / Kruskal-Wallis |
| Pearson相关 | 双变量正态 | 改用Spearman |
| 因子分析 | KMO>0.6、Bartlett显著 | 删除低相关变量后重试 |
| K-Means | 变量已标准化、样本量>10×K | 先标准化 |
---
## 模块五：SPSS语法
#### 5.1 语法基础规则
#### 5.2 常用语法模板库（可直接复制修改）
### 数据读取与保存
```spss
* 读取Excel数据.
GET DATA /TYPE=XLSX /FILE='C:\data\data.xlsx' /SHEET=name 'Sheet1'.
* 保存数据文件.
SAVE OUTFILE='C:\data\cleaned.sav'.
* 描述统计.
DESCRIPTIVES VARIABLES=x1 x2 x3 y
/STATISTICS=MEAN STDDEV MIN MAX SKEWNESS KURTOSIS.
* 标准化并生成Z变量.
DESCRIPTIVES VARIABLES=x1 x2 x3 /SAVE.
REGRESSION
/DESCRIPTIVES MEANS STDDEV CORR SIG N
/MISSING LISTWISE
/STATISTICS COEFF OUTS R ANOVA COLLIN TOL CHANGE
/CRITERIA=PIN(.05) POUT(.10)
/NOORIGIN
/DEPENDENT y
/METHOD=ENTER x1 x2 x3
/SCATTERPLOT=(*ZRESID ,*ZPRED)
/RESIDUALS DURBIN HISTOGRAM(ZRESID) NORMPROB(ZRESID).
REGRESSION
/MISSING LISTWISE
/STATISTICS COEFF OUTS R ANOVA COLLIN TOL
/DEPENDENT y
/METHOD=STEPWISE x1 x2 x3 x4 x5.
LOGISTIC REGRESSION VARIABLES y_bin
/METHOD=ENTER x1 x2 x3
/PRINT=CI(95) GOODFIT
/CRITERIA=PIN(0.05) POUT(0.10) ITERATE(20) CUT(0.5).
* 独立样本t检验.
T-TEST GROUPS=group(1 2) /VARIABLES=y /CRITERIA=CI(.95).

相关分析
聚类分析
因子分析
信度分析
### 5.3 批处理与复现技巧
国赛实战价值：赛前把常用分析写成 .sps 文件带到赛场，换数据只需改文件路径和变量名，30秒复现全套分析。
## 模块六：与其他工具对比
### 6.1 四工具对比表
* 单因素ANOVA + Tukey事后.
ONEWAY y BY group
/STATISTICS DESCRIPTIVES HOMOGENEITY
/POSTHOC=TUKEY ALPHA(0.05).
* 卡方检验.
CROSSTABS TABLES=gender BY opinion
/STATISTICS=CHISQ
/CELLS=COUNT EXPECTED.
* Pearson.
CORRELATIONS VARIABLES=x1 x2 x3 y /PRINT=TWOTAIL NOSIG.
* Spearman（非正态时用）.
NONPAR CORR VARIABLES=x1 x2 x3 y /PRINT=SPEARMAN TWOTAIL NOSIG.
* K-Means（变量须先标准化）.
QUICK CLUSTER zx1 zx2 zx3
/MISSING=LISTWISE
/CRITERIA=CLUSTER(3) MXITER(50) CONVERGE(0)
/METHOD=KMEANS(NOUPDATE)
/SAVE CLUSTER(cluster_id)
/PRINT INITIAL ANOVA.
* 系统聚类（Ward法）.
CLUSTER zx1 zx2 zx3
/METHOD=WARD
/MEASURE=SEUCLID
/PRINT SCHEDULE
/PLOT DENDROGRAM
/SAVE CLUSTER(3).
FACTOR
/VARIABLES x1 x2 x3 x4 x5 x6
/PRINT INITIAL KMO EXTRACTION ROTATION
/FORMAT=SORT BLANK(.5)
/PLOT EIGEN
/CRITERIA=MINEIGEN(1) ITERATE(25)
/EXTRACTION PC
/ROTATION VARIMAX
/SAVE REG(ALL).
RELIABILITY
/VARIABLES=q1 q2 q3 q4 q5
/SCALE('总量表') ALL
/MODEL=ALPHA
/SUMMARY=TOTAL.
* 一次性运行整个语法文件.
INSERT FILE='C:\analysis\all_steps.sps'.
* 循环处理（对y1到y5分别做回归）.
DEFINE !multi ()
!DO !i = 1 !TO 5.
REGRESSION /DEPENDENT !CONCAT('y',!i) /METHOD=ENTER x1 x2 x3.
!DOEND.
!ENDDEFINE.
!multi.
* 导出结果到Word/Excel（论文用）.
OUTPUT EXPORT
/CONTENTS=EXPORTALL
/DOCX DOCUMENTFILE='C:\output\结果.docx'.

维度
SPSS
Python
MATLAB
Excel
上手难度
⭐（点选）
⭐⭐⭐
⭐⭐⭐
⭐
传统统计检验
⭐⭐⭐⭐⭐
⭐⭐⭐⭐
⭐⭐⭐
⭐⭐
回归+诊断
⭐⭐⭐⭐⭐
⭐⭐⭐⭐
⭐⭐⭐⭐
⭐⭐
机器学习
⭐⭐
⭐⭐⭐⭐⭐
⭐⭐⭐⭐
⭐
优化算法
⭐
⭐⭐⭐⭐⭐
⭐⭐⭐⭐⭐
⭐⭐
大数据
⭐⭐
⭐⭐⭐⭐⭐
⭐⭐⭐⭐
⭐
论文级表格输出
⭐⭐⭐⭐⭐
⭐⭐⭐
⭐⭐⭐
⭐⭐⭐
问卷分析
⭐⭐⭐⭐⭐
⭐⭐⭐
⭐⭐
⭐⭐
### 6.2 国赛黄金组合工作流
数据互通方式：SPSS 中 文件→另存为→CSV，即可交给 Python（pandas）继续处理。
### 6.3 切换信号（什么时候放弃SPSS）
信号
应切换到
数据超过 50 万行、操作卡顿
Python
需要随机森林/XGBoost/神经网络
Python
需要遗传算法、模拟退火、整数规划
MATLAB / Python
需要偏最小二乘、岭回归等高级回归
Python
或 SPSS+插件
需要复杂自定义指标计算
Python / MATLAB
队员不熟统计、只需标准检验出表
✅ 留在 SPSS
反向下沉信号：只是做个均值、频数、简单透视 → 用 Excel 更快，别开 SPSS。
## 附录：赛前速查卡
### 一分钟检查清单
- 数据已导入，变量视图已命名并设好类型/度量
- 缺失值已处理（注明方法）
- 离群值已检测（注明处理策略）
- 回归前：VIF已勾选、DW已勾选、残差图已设置
- 论文中：三线表 ✅、图表编号 ✅、正文引用 ✅、软件版本已注明 ✅
- p=0.000 已改写为 p<0.001
- 分析流程已保存为 .sps 语法文件（可复现）
### 高频判断标准背诵
万行+
【SPSS + Python 组合队】（最常见，推荐）
1. Excel/CSV 原始数据
↓
SPSS：清洗、描述统计、检验变量显著性、回归基线、
因子定权重、聚类分群 → 输出结论写入论文
↓ (导出处理后的 .csv)
Python：随机森林/XGBoost预测、遗传/退火优化、
神经网络、复杂仿真 → 输出预测与优化结果
↓
4. 统一写入论文（表图规范化）
pandas
sklearn
sklearn
p<0.05 显著 | R²>0.7 拟合好 | VIF<5 无共线性 | DW≈2 无自相关
KMO>0.8 很好 | 累计方差>60% | α>0.7 信度可接受 | 载荷>0.5 归属因子

---
## 文件使用说明
| 项目 | 说明 |
|------|------|
| **保存为** | `SKILL.md`（或 `spss-mathmodeling.md`） |
| **使用方式** | ① 团队知识库文档，赛前通读<br>② 作为 AI 的 Skill 文件加载，问答时自动参考<br>③ 打印模块二+附录作为赛场速查卡 |
| **如需 JSON 版** | 可将上述结构转换为 JSON 格式（机器可读），告诉我即可转换 |
**核心记忆点**：这份 Skill 的六大模块对应完整的国赛工作流 —— **选方法(四) → 做操作(一/五) → 读结果(二) → 写论文(三)**，工具决策(六)贯穿始终。