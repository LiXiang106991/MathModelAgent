# 数学建模国赛论文「模型建立」书写模板

> 基于2020-2024年国赛优秀论文（A/B/C题国一）的写作规律提炼
> 版本: v1.0 | 更新: 2026-08-29

---

## 一、总体原则

### 1.1 书写逻辑链

```
问题分析 → 模型选择依据 → 模型假设/约定 → 符号定义 → 
模型建立（公式推导） → 模型求解算法 → 结果呈现
```

### 1.2 核心要求

| 要求 | 说明 | 常见错误 |
|------|------|---------|
| **先文字后公式** | 每段先写"做什么"，再写"怎么做" | 直接堆公式，无文字过渡 |
| **先整体后局部** | 先给框架，再给细节 | 陷入细节，读者迷失 |
| **先简单后复杂** | 基础模型→改进模型 | 一上来就是最复杂版本 |
| **公式必有解释** | 每个符号首次出现必须说明 | 符号未定义直接使用 |
| **模型必有依据** | 说明为什么选这个模型 | "根据经验""众所周知" |

---

## 二、标准结构模板

### 2.1 章节标题格式

```
五、问题X的建模与求解
5.1 子问题一：XXXXX
    5.1.1 模型的建立
    5.1.2 模型的求解
    5.1.3 结果分析
5.2 子问题二：XXXXX
    ...
5.3 子问题三：XXXXX
    ...
```

> 注：若问题间关联紧密，可用"5.1 数据预处理→5.2 模型建立→5.3 结果分析"的纵向结构

---

## 三、「模型的建立」详细模板

### 3.1 第一步：问题转化（必写！）

**模板句式**：
> "问题X要求【做什么】，本质上是一个【什么类型】问题。考虑到【数据特征/约束条件/问题特点】，本文将其转化为【数学描述】。"

**示例**（2022C题问题一）：
> "问题一要求分析风化与类型、纹饰、颜色的关系，并预测风化前成分。考虑到成分数据为闭合约束下的比例变量，且风化效应可能因玻璃类型而异，本文将第一问转化为**分类型计数关联分析**与**带交互项的线性修正**两个问题分别处理。"

---

### 3.2 第二步：模型选择依据

**模板句式**：
> "对于【问题类型】，常用的方法有【方法A】、【方法B】和【方法C】。其中，【方法A】适用于【场景1】，但【局限1】；【方法B】适用于【场景2】，但【局限2】。鉴于本题【具体条件】，本文选用【方法D】，原因是【优势1、2、3】。"

**示例**（分类问题）：
> "对于高钾/铅钡二分类问题，常用的方法有决策树、支持向量机和神经网络。其中，神经网络需要大量训练样本，本题仅56件文物，样本量不足；支持向量机的核函数选择缺乏明确准则。鉴于本题**小样本、高维特征、需要可解释规则**的特点，本文选用**决策树（CART）**作为主分类器，同时辅以随机森林、Logistic回归和LDA进行交叉验证，以确保结论稳健。"

---

### 3.3 第三步：符号定义（独立成段或表格）

**推荐格式**：文字叙述中首次出现即定义，或前置符号说明表

```markdown
设【对象集合】为$\Omega = \{\omega_1, \omega_2, ..., \omega_n\}$，其中：
- $n$：【含义】，单位【单位】；
- $\omega_i$：【含义】，$i = 1,2,...,n$；
- $x_{ij}$：第$i$个样本的第$j$个特征值，$j = 1,2,...,p$。
```

**禁忌**：
- ❌ "设$x$为..."（$x$是什么类型的变量？范围？）
- ✅ "设$x \in \mathbb{R}^+$为第$i$个采样点的SiO₂含量（%），满足$\sum_{j=1}^{12} x_j \approx 100$"

---

### 3.4 第四步：模型建立（核心公式推导）

#### 3.4.1 基础模型（先给最简单的形式）

**模板**：
```markdown
【模型名称】的基本形式为：

$$\text{【目标/方程】} = \text{【表达式】}$$

其中：
- 【符号1】：【含义】；
- 【符号2】：【含义】。
```

**示例**（线性回归）：
```markdown
对于成分$c$，建立风化程度$d$与观测含量$X_c$的线性关系：

$$X_c = \beta_0 + \beta_1 d + \varepsilon$$

其中：
- $X_c$：成分$c$的观测含量（%）；
- $d$：风化程度，$d \in \{0,1,2\}$，分别对应未风化、普通风化、严重风化；
- $\beta_0$：未风化时的基准含量；
- $\beta_1$：每增加一级风化的平均变化量；
- $\varepsilon$：随机误差项，假设$\varepsilon \sim N(0, \sigma^2)$。
```

#### 3.4.2 改进模型（针对题目特点的扩展）

**模板**：
```markdown
然而，上述基础模型未考虑【题目中的某个关键因素】。根据问题分析，【因素】对【结果】有显著影响。因此，本文在基础模型中引入【改进项】，得到改进模型：

$$\text{【改进后的公式】}$$

其中新增符号：
- 【符号】：【含义及作用】。

【改进的合理性说明：为什么加这一项？不加会怎样？】
```

**示例**（带类型交互的线性回归）：
```markdown
然而，基础模型未考虑玻璃类型对风化效应的调节作用。根据5.2节的分析，铅钡与高钾的风化效应方向相反（铅钡SiO₂流失，高钾SiO₂富集），若不分类型统一建模，类型差异将被误判为风化效应。

因此，引入类型示性变量$I_{\text{铅钡}}$（铅钡=1，高钾=0）及其与风化程度的交互项，建立带类型交互的线性模型：

$$X_c = \beta_0 + \beta_1 I_{\text{铅钡}} + \beta_2 d + \beta_3 I_{\text{铅钡}} \cdot d + \varepsilon$$

其中新增符号：
- $I_{\text{铅钡}}$：类型示性变量，铅钡玻璃取1，高钾玻璃取0；
- $\beta_2$：高钾玻璃的风化斜率（$I_{\text{铅钡}}=0$时$d$的系数）；
- $\beta_2 + \beta_3$：铅钡玻璃的风化斜率（$I_{\text{铅钡}}=1$时$d$的系数）。

该模型的合理性在于：通过交互项$\beta_3$捕捉类型对风化效应的调节，使得两类玻璃拥有独立的风化斜率。若$\beta_3$不显著（$p>0.05$），则退化为基础模型，说明类型调节作用不存在。
```

#### 3.4.3 目标函数与约束（优化类问题必写）

**模板**：
```markdown
【问题】可表述为如下【优化类型】问题：

$$\begin{aligned}
& \min_{\boldsymbol{x}} / \max_{\boldsymbol{x}} && f(\boldsymbol{x}) = \text{【目标函数】} \\
& \text{s.t.} && g_i(\boldsymbol{x}) \leq 0, \quad i = 1,2,...,m \\
& && h_j(\boldsymbol{x}) = 0, \quad j = 1,2,...,l \\
& && x_k \in \mathbb{Z} / \{0,1\} / \mathbb{R}^+, \quad k = 1,2,...,n
\end{aligned}$$

其中：
- 【逐条解释每个约束的物理/实际意义】
```

**示例**（背包问题）：
```markdown
文物选择问题可表述为0-1整数规划：

$$\begin{aligned}
& \max_{\boldsymbol{x}} && Z = \sum_{i=1}^{n} v_i x_i \\
& \text{s.t.} && \sum_{i=1}^{n} w_i x_i \leq W \\
& && x_i \in \{0,1\}, \quad i = 1,2,...,n
\end{aligned}$$

其中：
- $x_i$：是否选择第$i$件文物，$x_i=1$表示选择，$x_i=0$表示不选；
- $v_i$：第$i$件文物的研究价值；
- $w_i$：第$i$件文物的检测成本；
- $W$：总预算上限；
- 目标函数$Z$：总研究价值最大化；
- 约束1：总成本不超过预算；
- 约束2：决策变量为0-1整数。
```
---

## 四、不同题型的模型建立要点

### 4.1 预测类问题

```markdown
5.X 预测模型的建立

5.X.1 数据预处理
    - 缺失值处理
    - 异常值检测
    - 标准化/归一化

5.X.2 特征工程（如需要）
    - 特征选择
    - 特征构造
    - 降维

5.X.3 预测模型
    【模型名称】的基本原理是...本文选用该模型是因为...

    【基础模型公式】

    【针对本题特点的改进】

    【参数设置及依据】

5.X.4 模型评估指标
    - RMSE/MAE/MAPE/R²（回归）
    - Accuracy/Precision/Recall/F1/AUC（分类）
```

### 4.2 优化类问题

```markdown
5.X 优化模型的建立

5.X.1 问题分析
    【将实际问题抽象为数学优化问题】

5.X.2 决策变量定义
    【明确每个变量代表的实际意义】

5.X.3 目标函数建立
    【最大化/最小化什么？为什么？】

5.X.4 约束条件分析
    【逐条列出约束，并说明实际来源】
    - 等式约束
    - 不等式约束
    - 整数/0-1约束
    - 非负约束

5.X.5 完整模型表述
    【用标准优化形式写出】

5.X.6 求解算法选择
    【为什么选这个算法？复杂度如何？】
```

### 4.3 评价类问题

```markdown
5.X 评价模型的建立

5.X.1 评价指标体系构建
    - 指标选取原则（全面性、独立性、可操作性）
    - 指标分层（目标层→准则层→指标层）

5.X.2 指标量化与标准化
    - 正向指标、负向指标、适度指标的处理
    - 标准化方法选择及依据

5.X.3 权重确定
    - 主观赋权：AHP / 德尔菲
    - 客观赋权：熵权法 / CRITIC / 变异系数
    - 组合赋权（如需要）

5.X.4 综合评价模型
    - TOPSIS / 灰色关联 / 模糊综合 / 物元可拓
    【模型公式及解释】
```

---

## 五、常见错误与规避

| 错误类型 | 典型案例 | 正确做法 |
|---------|---------|---------|
| **公式空降** | 直接写"根据XX模型："然后堆公式 | 先说明"为什么要用这个模型" |
| **符号混乱** | 同一符号在不同公式中含义不同 | 全文符号表统一，首次定义 |
| **假设缺失** | 线性回归不写误差分布假设 | 明确写出$\varepsilon \sim N(0,\sigma^2)$及依据 |
| **改进无据** | "为了更精确，加入交互项" | "根据5.2节分析，两类效应方向相反，故引入交互项" |
| **算法堆砌** | 列出5种算法但不说明选择依据 | 对比各算法适用性，明确选择理由 |
| **结果前置** | 模型建立部分就出现具体数值 | 数值放在"模型的求解"部分 |

---

## 六、优秀论文金句摘录（可直接化用）

### 6.1 模型选择
> "考虑到本题【小样本/高维/非线性/动态】的特点，【方法A】的【优势】恰好弥补【方法B】的【不足】，故本文选用..."

> "【方法】虽然【优点】，但其【假设/前提】在本题中【是否满足】。经检验，【数据特征】，因此【结论】。"

### 6.2 模型改进
> "上述基础模型隐含了【假设】，但5.X节的分析表明该假设不成立。为此，本文引入【改进项】，将模型扩展为..."

> "若【参数】不显著（$p>0.05$），则改进项可忽略，模型退化为【基础形式】，说明【结论】。"

### 6.3 合理性说明
> "该模型的合理性在于：（1）...；（2）...；（3）..."

> "从【领域】角度看，【公式/参数】具有明确的物理/化学/经济意义：..."

---

## 七、附录：完整段落示例

### 示例：带类型交互的线性回归（2022C题问题一）

```markdown
### 5.4.1 模型的建立

问题一第三问要求依据风化点的检测数据预测其风化前的化学成分含量。
由于风化过程导致成分比例变化，直接以风化后数据作为配方依据会引入
系统性偏差。因此，需要建立风化效应的定量修正模型。

**基础模型**：假设风化对成分的影响近似线性，建立单因素线性回归：

$$X_c = \beta_0 + \beta_1 d + \varepsilon$$

其中$X_c$为成分$c$的观测含量（%），$d$为风化程度（0/1/2），$\beta_1$
为风化斜率。该模型隐含"所有玻璃风化效应相同"的假设。

**假设检验**：然而，5.3节的Mann-Whitney U检验表明，铅钡与高钾的
风化效应方向截然相反——铅钡风化后SiO₂流失（-28.02%），高钾风化后
SiO₂相对富集（+25.98%）。若忽略类型差异统一建模，类型效应将被误判
为风化效应，导致预测偏差。

**改进模型**：引入类型示性变量$I_{\text{铅钡}}$及其与风化程度的交互项：

$$X_c = \beta_0 + \beta_1 I_{\text{铅钡}} + \beta_2 d + \beta_3 I_{\text{铅钡}} \cdot d + \varepsilon$$

其中：
- $I_{\text{铅钡}}$：类型示性变量，铅钡玻璃取1，高钾玻璃取0；
- $\beta_2$：高钾玻璃的风化斜率；
- $\beta_2 + \beta_3$：铅钡玻璃的风化斜率；
- $\beta_3$：类型对风化效应的调节量，若$\beta_3$显著不为0，则证实
  类型调节作用存在。

**风化前成分预测**：对于风化程度为$d$的样本，其风化前成分估计为：

$$\hat{X}_{c,\text{前}} = X_{c,\text{观测}} - \hat{\beta}_{\text{type}} \cdot d$$

其中$\hat{\beta}_{\text{type}}$根据玻璃类型取$\hat{\beta}_2$（高钾）或
$\hat{\beta}_2 + \hat{\beta}_3$（铅钡）。

**模型合理性**：
1. 线性假设：5.4.2节以决策树回归效果量对比验证，线性斜率与非线性
   效果量方向一致；
2. 分类型处理：避免类型-风化混淆，符合5.2节的统计结论；
3. 可验证性：49/50两件真配对样本可用于留一交叉验证。
```

---

> **记忆口诀**：问题转化→选择依据→符号定义→基础模型→改进扩展→求解算法→结果衔接。
> 
> **核心原则**：让读者不看代码也能复现你的模型。
