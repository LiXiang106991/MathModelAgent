# 数据应对手册（数学建模国赛版）

> **本手册是"战术手册"**：教你怎么走数据处理的完整流程，任何赛题、任何年份都能照着做。
> 工具命令的完整参考在 [tools/README.md](tools/README.md)；规范细则（防幻觉、一致性、图表、摘要）在 [math_modeling_norms.md](math_modeling_norms.md)；全流程管线见根目录 [README.md](README.md) 的 6 技能流。
> 数据工作在管线中的位置：**阶段 1（读题）与阶段 2（`/2analysis-modeling` 数据理解）之间，以及整个建模过程中**。

---

## 0. 速览卡（30 秒定位）

| 阶段 | 目标 | 关键动作 | 产物 | 链接 |
|---|---|---|---|---|
| 赛前 | 工具就绪 | 自检 `test_tools.py`、装 openpyxl | 环境可用 | 第 2 节 |
| 第一天 1-2h | 数据盘库转格式 | 附件→CSV→按需合并 | 各表 CSV + 数据字典.md | 第 3 节 |
| 探索 | 让 AI 看懂全貌 | 每表跑画像生成器 | 数据画像.md | 第 4 节 |
| 建模 | 按需取精确数 | `query_data` 查数，只带回结果 | 结果登记表 | 第 5 节 |
| 写论文 | 数字唯一来源 | 所有数字从登记表复制 | 论文定稿 | 第 7 节 |
| 交卷前 | 数据侧终检 | 数字可追溯、行数一致、可复现 | 通过核对 | 第 8 节 |

**三条铁律**
1. **全量数据永远不整包进对话** —— 对话里只有画像（全貌）+ 数据字典（含义）+ 查询结果（精确数）。
2. **AI 报出的每个数字必须能指回一次 `query_data` 输出** —— 指不回去就当它没说过。
3. **画像 + 数据字典是 AI 理解数据的唯一入口** —— 数据字典的"业务含义"必须人来补，工具补不了。

---

## 1. 为什么不能把全量数据贴给 AI

一句话：**几万行的表直接喂进对话 ≈ 上千万 token，远超任何模型上下文，且必然引发幻觉。**

一份 9.6 万行的 CSV（2.8 MB）约等于 **1,100 万 token**；而它的画像只有 **4,500 token**（压缩约 2400 倍）。完整对比表见 [tools/README.md](tools/README.md)。

分工机制（记住这三个词）：

- **画像 = 全貌**：让 AI 一次看清全部数据的结构 + 统计规律（几秒生成一次）。
- **查询 = 精确数**：AI 建模时需要某个数字时，命令行按需取数，只把结果带进对话。
- **数字只用查询输出**：论文里的每个数字都来自代码输出，AI 不记忆、不编造。

---

## 2. 赛前准备（比赛前半天）

目标：避免比赛当天才发现工具没装好、没演练过。

- [ ] tools/ 四件套齐全（csv_io.py / gen_data_profile.py / query_data.py / test_tools.py）
- [ ] `python tools/test_tools.py` 全部 PASS（冒烟覆盖：编码识别、截断、画像、全部子命令）
- [ ] 已装 pandas + openpyxl（读 xlsx 必需）：`pip install pandas openpyxl`
- [ ] 环境检查：`/doctor` 通过（python / typst / xelatex 依赖）
- [ ] 建好目录骨架：`data/ results/ code/ reports/ paper/`，原件统一放 `data/raw/` 备份
- [ ] 打印"附录 9.1 命令速查卡"；三人分工确认（取数人 / 建模人 / 论文人）

> 提示：比赛当天不要现场装环境。提前用往年赛题附件完整演练一遍第 3~5 节流程。

---

## 3. 拿到题目后 1~2 小时：盘库与转格式

工具只认 CSV，所以**第一道硬门槛是把附件转成 CSV 并归档**。核心是"盘库 → 转 CSV → 按需合并 → 写数据字典"。

### 3.1 数据盘库清单

- [ ] 列出全部附件：格式（xlsx/csv/txt）、每个 Excel 的 sheet 数、行列数
- [ ] 标出每张表的字段角色：**主键**（唯一标识行）、**关联键**（用来 join 别的表）、数值列、类别列、时间列
- [ ] 判定是否表格型数据（行列结构）；不是 → 见 6.4 节
- [ ] 判定哪些表需要合并、合并键是什么（如 作物编号 / 地块名称）

### 3.2 Excel → CSV（两种方案）

**方案 A：Excel 手动另存为 CSV**。每个 sheet 分别另存。注意中文版 Excel 可能存成 GBK —— 我们的工具会自动识别编码，所以也能读；但统一用方案 B 更稳。

**方案 B：pandas 一行转换，统一写 utf-8-sig**（推荐，避免乱码）：

```bash
# 单个 sheet
python -c "import pandas as pd; pd.read_excel('附件2.xlsx', sheet_name='2023年统计的相关数据').to_csv('data/附件2_统计.csv', index=False, encoding='utf-8-sig')"

# 一次性转全部 sheet（每个 sheet 存一个 CSV）
python -c "import pandas as pd; [df.to_csv(f'data/附件2_{name}.csv', index=False, encoding='utf-8-sig') for name, df in pd.read_excel('附件2.xlsx', sheet_name=None).items()]"
```

> **例1 转格式**：把 2024 C题 `附件2.xlsx` 的"2023年统计的相关数据"sheet 转成 `data/附件2_统计.csv`，即上面第一条命令。命名规范 `附件N_sheet名.csv`，一眼能对应回原件。
> （示例列名均以实际附件的 `schema` 输出为准，下同。）

- [ ] 全部附件已转 CSV 并存进 `data/`（每 sheet 一个）
- [ ] 记录原始行数与处理后行数（写论文"数据处理"小节要用）

### 3.3 多表合并（若需要）

用 pandas `merge` 按关联键拼表。先确认主键唯一性，避免一对多把行数翻倍。

```bash
python -c "
import pandas as pd
地块 = pd.read_excel('附件1.xlsx', sheet_name='乡村的现有耕地')
作物 = pd.read_excel('附件1.xlsx', sheet_name='乡村种植的农作物')
种植 = pd.read_excel('附件2.xlsx', sheet_name='2023年的农作物种植情况')
合并 = 种植.merge(地块, left_on='种植地块', right_on='地块名称').merge(作物, on='作物编号')
合并.to_csv('data/种植_合并.csv', index=False, encoding='utf-8-sig')
print('合并后行数:', len(合并))
"
```

> 建议：**原始表各自先出画像，合并后再出一张画像**。两张画像配合看，能发现"合并引入了什么、丢了什么"。

### 3.4 写数据字典

字段的业务含义工具推断不了，必须人补。**数据字典是 AI 理解数据的入口**，一定要写。

- [ ] 用 `schema` 输出当草稿：`python tools/query_data.py schema data/种植_合并.csv`
- [ ] 对每个字段补三列：**含义、单位、备注**（备注写缺失口径、异常标记）
- [ ] 存成 `data/数据字典.md`，例如：

| 字段 | 含义 | 单位 | 备注 |
|---|---|---|---|
| 作物编号 | 作物唯一编号 | — | 关联附件1 作物表的主键 |
| 种植面积/亩 | 该地块该作物种植面积 | 亩 | 0 表示当年未种 |
| 种植季次 | 第一/第二季 | — | 大棚可多季 |

- [ ] 原始与处理后行数、缺失/异常口径一并记入数据字典

---

## 4. 探索性分析：画像先行（约 30 分钟）

目标：让 AI 一次看清全貌。这正好是 `/2analysis-modeling` 里"数据理解"这一步的输入。

### 4.1 生成画像

```bash
python tools/gen_data_profile.py data/种植_合并.csv --label "2023种植合并" --group 作物类型 --out 数据画像_种植合并.md
```

参数怎么选：`--label` 写表名；`--group` 写你关心的分组列（逗号分隔多列）；`--top` 限制类别列频数显示条数（默认 20，超大表改小）。

> **例2 合并 + 画像**：对 3.3 生成的 `data/种植_合并.csv` 跑上面命令。画像"数据概览"应显示 **~87 行**（与 2023 种植情况 sheet 行数一致），重点核对"作物类型"频数和"种植面积"总和量级是否合理。

### 4.2 画像 7 区块评审清单（人肉过一遍）

- [ ] **数据概览**：行数列数与题面对得上
- [ ] **数值列分布**：总和/量级/零值占比合理吗？（如"面积"不应大面积为零）
- [ ] **类别列取值**：有无乱码、前后空格、同名不同值（`A-1` vs `A1`）
- [ ] **相关性矩阵**：是否暴露完全共线（两列几乎相同 → 信息重复，建模前删一列）
- [ ] **时间列分析**：跨度/粒度够不够建模需求
- [ ] 异常列在数据字典里标红注明

### 4.3 打包发给 AI

发送内容 = **赛题描述.md + 数据字典.md + 每张原始表画像 + 合并表画像**。这几份加起来几千 token，AI 一次看完不烧钱、不幻觉。

> AI 建模报告若出现画像里没有的数字，直接质疑：让它指出查询来源。

---

## 5. 建模过程中：按需取数

AI 要精确数字就走进这里。铁律：**查询结果贴回对话，原始 CSV 不贴**。

### 5.1 需求 → 子命令 对照表

| 我想…… | 子命令 | 一行示例 |
|---|---|---|
| 看样本 | `head` | `query_data head 种植_合并.csv -n 5` |
| 看列类型/唯一值/缺失 | `schema` | `query_data schema 种植_合并.csv` |
| 数值分布 | `describe` | `query_data describe 种植_合并.csv --cols 种植面积/亩` |
| 缺失/零值占比 | `missing` | `query_data missing 种植_合并.csv` |
| 分组聚合 | `group` | `query_data group 种植_合并.csv --by 作物类型 --agg "种植面积/亩:sum,mean"` |
| 按条件取行 | `filter` | `query_data filter 种植_合并.csv --where "作物类型 == '粮食'" -n 20` |
| 相关系数 | `corr` | `query_data corr 种植_合并.csv` |
| 计数 | `count` | `` query_data count 种植_合并.csv --where "`种植面积/亩` == 0" `` |
| 任意 pandas | `expr` | `query_data expr 种植_合并.csv "df['种植面积/亩'].max()"` |

> **列名含 `/`、空格等特殊字符时必须用反引号包住**，否则 pandas 会把 `/` 当除法。例：`` --where "`种植面积/亩` == 0" ``。完整命令参数见 [tools/README.md](tools/README.md)。

### 5.2 与 AI 协作取数约定

1. AI 提出需求（要哪列、什么条件）—— 它负责想，不负责编数。
2. 取数人跑 `query_data` 得到输出。
3. 把输出（或关键几行）贴回对话，AI 基于真实数字继续。
4. 进论文的数字登记进**结果登记表**（见下）。

> **例3 按需取数**：AI 需要"粮食类作物的种植面积总和"，你跑：
> ```bash
> python tools/query_data.py filter data/种植_合并.csv --where "作物类型 == '粮食'" --cols 作物编号,作物名称,种植面积/亩 -n 20
> python tools/query_data.py group data/种植_合并.csv --by 作物类型 --agg "种植面积/亩:sum,mean"
> ```
> 这两条输出里的数字就是论文里的数字。记进结果登记表。

**结果登记表**（第 5 节全程维护，放 `reports/`）：

| 数字 | 来源命令 | 用途（章节） | 日期 |
|---|---|---|---|
| 87（种植总行数） | 画像·数据概览 | 数据处理 | 第1天 |

### 5.3 Python vs SPSS 分工

- 简单预处理（缺失填充、异常识别、标准化）→ 用 SPSS 或 Python 都行，**选你熟的那个**；SPSS 操作见 [SPSS 手册](<SPSS_数学建模国赛技能手册(1).md>) 预处理部分。
- 大表、机器学习、优化求解 → 必须 Python。
- 最终判定听 [SPSS 手册](<SPSS_数学建模国赛技能手册(1).md>) 的"题型→方法"映射。

---

## 6. 边界情况与疑难数据

### 6.1 超大表 / 超多类别列
- 画像类别列频数默认显示前 20，超大表把 `--top` 调小。
- 类别唯一值上千（如逐条记录 ID）时，**先 `group` 聚合再画像**，别让画像爆炸。

### 6.2 编码乱码（GBK / UTF-8 / BIG5）
- 工具自动识别编码；看到乱码先重新 `pd.to_excel→csv` 写 utf-8-sig，或用 Excel 另存 CSV。
- 排查：`python -c "import sys; sys.path.insert(0, 'tools'); import csv_io; print(csv_io.detect_encoding('文件.csv'))"`

### 6.3 时间列 / 长文本列
- 时间列画像自动识别（跨度/粒度）。
- 长文本列（备注、说明）不进画像，看频数即可；真要用就 `filter` 或 `expr` 按行取。

### 6.4 非表格附件（图片 / PDF / 地图）
- 不进本管线。截图/图片 → 描述成文字；PDF → 抽取关键段落并**二次核对**（机器抽取常有错）。规范见 [math_modeling_norms.md](math_modeling_norms.md) 赛题理解部分。

### 6.5 缺失 / 异常 / 重复
- **定位**用 `count` / `filter`（如 `` count --where "`种植面积/亩` == 0" ``）。
- **处理口径**（删行？填 0？插值？）记入数据字典备注，论文要写。
- **方法本身**不在这里重写：缺失/异常/标准化的操作见 [SPSS 手册](<SPSS_数学建模国赛技能手册(1).md>) 预处理部分。

---

## 7. 写论文时：数字的唯一来源

- [ ] 论文里的每个数字都来自**结果登记表**（第 5 节），不凭记忆、不手敲估算
- [ ] **摘要最后写**，数值从正文复制（呼应 norms"摘要最后写"）
- [ ] 每张图由真实结果数据生成，保留数据来源；图表规范见 [math_modeling_norms.md](math_modeling_norms.md)
- [ ] "数据处理"小节写清：数据来源、转格式过程、合并口径、缺失/异常处理口径
- [ ] 正文**不出现内部文件名/脚本名**（`data/附件2_统计.csv` 这类），只写业务表名

---

## 8. 交卷前核对（数据侧终检清单）

与 `/6verity` 验收配合，这是**数据专项**终检：

- [ ] 每个数字能指回一次查询输出 / 结果登记表
- [ ] 论文提到的行数与画像、数据字典一致
- [ ] 附件清单与数据字典表一一对应，无漏表
- [ ] CSV 编码规范、命名规范统一
- [ ] `data/ + code/ + results/` 放齐，命令可复现
- [ ] 与 6verity 生成的 `VERIFY_REPORT` 对照通过

---

## 9. 附录

### 9.1 命令速查卡

```bash
# 画像（核心）
python tools/gen_data_profile.py 表.csv --out 画像.md --label 表名 --group 分组列,分组列

# 取数（常用 6 条）
python tools/query_data.py schema 表.csv
python tools/query_data.py describe 表.csv --cols 数值列
python tools/query_data.py group 表.csv --by 类别列 --agg "数值列:sum,mean"
python tools/query_data.py filter 表.csv --where "类别列 == '值'" -n 20
python tools/query_data.py count 表.csv --where "条件"
python tools/query_data.py expr 表.csv "df['列'].mean()"

# 自检
python tools/test_tools.py
```

### 9.2 资产关系图

```
数据应对手册（本文件：战术，怎么走流程）
 ├─ 命令细节 ──────→ tools/README.md（画像 + 查询助手完整用法）
 ├─ 规范细则 ──────→ math_modeling_norms.md（防幻觉/一致性/图表/摘要）
 ├─ 预处理方法 ────→ SPSS_数学建模国赛技能手册(1).md（缺失/异常/标准化）
 └─ 阶段衔接 ──────→ README.md（6 技能流：1start→2analysis→3coding→4drawio→5writing→6verity）
```

**一句话提醒：数据是阵地，数字是弹药。弹药只从 `query_data` 里领，AI 从来不产弹药。**
